{
  "directories": {
    "test": "test"
  },
  "author": "azu",
  "license": "MIT",
  "files": [
    "src/",
    "lib/",
    "bin/"
  ],
  "name": "eslint-plugin-no-allow-react-context",
  "version": "1.0.0",
  "description": "Not allow to use React context in specific directory.",
  "main": "lib/index.js",
  "scripts": {
    "test": "mocha test/",
    "build": "NODE_ENV=production babel src --out-dir lib --source-maps",
    "watch": "babel src --out-dir lib --watch --source-maps",
    "prepublish": "npm run --if-present build"
  },
  "keywords": [
    "react",
    "eslint"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/azu/eslint-plugin-no-allow-react-context.git"
  },
  "bugs": {
    "url": "https://github.com/azu/eslint-plugin-no-allow-react-context/issues"
  },
  "homepage": "https://github.com/azu/eslint-plugin-no-allow-react-context",
  "devDependencies": {
    "babel-cli": "^6.14.0",
    "babel-eslint": "^6.1.2",
    "babel-preset-es2015": "^6.14.0",
    "babel-preset-jsdoc-to-assert": "^4.0.0",
    "babel-preset-power-assert": "^1.0.0",
    "babel-register": "^6.14.0",
    "eslint": "^3.4.0",
    "mocha": "^3.0.2",
    "power-assert": "^1.4.1"
  },
  "dependencies": {
    "minimatch": "^3.0.3"
  }
}
